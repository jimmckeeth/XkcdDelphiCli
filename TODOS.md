# Pending Tasks and Future Features

## Phase 4: Polish & Interaction
- [ ] Implement `--no-terminal-scale-up` flag to prevent upscaling smaller comics.
- [ ] Implement `--picker`: Interactive selection menu using native SQLite data.
- [ ] Add more cache hardening for corrupted JSON and permission failures.

## Phase 5: Superpowers (Planned)
- [ ] Semantic search using vector embeddings (via `cocoindex` and `sqlite-vec`).
- [ ] Related comic discovery via FTS5/Vector similarity.
- [ ] Built-in Terminal User Interface (TUI) for comic browsing.
